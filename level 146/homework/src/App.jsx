import Prime from "./components/Prime";

const App = () => {

  // Task 1
  
  // Caching is a temporary space where information is stored, and with each new 
  // piece of information, the old information is replaced, so it is a temporary space because information is 
  // stored there temporarily so that, for example, certain actions do not go unnoticed.

  // Task 2

  return (
    <>
      <Prime />
    </>
  );

}

export default App
